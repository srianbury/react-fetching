export const MISSING_defaultError_MESSAGE = "No `defaultError` provided.  A `defaultError` must be provided to <FetchingContextProvider> to use the `error` prop as a boolean.";
export const MISSING_defaultLoader_MESSAGE = "No `defaultLoader` provided.  A `defaultLoader` must be provided to <FetchingContextProvider> to use the `loading` prop as a boolean.";
