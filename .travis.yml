dist: jammy

language: node_js
node_js:
  - lts/*

jobs:
  include:
    # Define the release stage that runs semantic-release
    - stage: release
      node_js: lts/*
      deploy:
        provider: script
        script:
          - yarn publish
        on:
          branches: main
